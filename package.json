{
    "name": "zapatoflex-monorepo",
    "version": "1.0.0",
    "description": "Repositorio raíz de ZapatoFlex para gestión de Backend y Frontend",
    "scripts": {
        "instalar": "npm install --prefix backend && npm install --prefix frontend",
        "tienda": "npm run dev --prefix frontend",
        "servidor": "npm run dev --prefix backend",
        "desarrollo": "concurrently \"npm run tienda\" \"npm run servidor\"",
        "dev": "npm run desarrollo"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}